# Screenshots — 0.1.9

Store finals in this folder. Prefer PNG. Keep originals if you crop.

**Demo:** `make app-builder` — Open Copilot → Connection.

## Required shots

| File | Scene | Why |
| --- | --- | --- |
| `hero.png` | Connection popover with **Config file** and **Connection** selectboxes on one row | LinkedIn + GitHub Release |
| `01-config-connection.png` | Same moment, cropped to the two selectboxes + environment probe | Shows file + profile together |

## Optional

| File | Scene |
| --- | --- |
| `demo.gif` or `demo.mp4` | 10–20s: open Connection → change Config file → Connection list updates |

## Capture tips

- Use a **fresh** session; hide personal connection names if sensitive
- Light theme unless dark is the product default you want to brand
- Crop to the popover; avoid the full App Builder chrome
- Same window width across a carousel (~1280px app width works well)
- Overlay text only in a separate marketing export — keep repo PNGs clean

## After capture

- [ ] Drop files here
- [ ] Reference paths from `doc-dev/releases/0.1.9/linkedin.md` / `medium.md` and the GitHub Release
