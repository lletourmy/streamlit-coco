# CSV explorer

Use when the type id is `csv-explorer`.

## Layout

1. Title + caption “local file · no Snowflake”
2. Filter widgets for requested columns
3. One chart (or none) + one dataframe

## Data

- Demo pack: `data.csv` in cwd (copied from the fixture).
- Never query Snowflake. Never invent extra files.
