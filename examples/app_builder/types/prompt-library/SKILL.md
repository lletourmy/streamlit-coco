# Prompt library

Use when the type id is `prompt-library`.

## Layout

1. Search or tag filter
2. One expander (or cards) per prompt: title, tags, body
3. Easy copy of the body

## Data

- Demo pack: `prompts.md` in cwd.
- Do not call an LLM just to list prompts. No Snowflake.
